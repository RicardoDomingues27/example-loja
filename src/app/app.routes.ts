import { Routes } from '@angular/router';
import { PrateleiraComponent } from '../pages/prateleira/prateleira.component';

export const routes: Routes = [
    { path: '', component: PrateleiraComponent },
];
